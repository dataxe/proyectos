ALTER TABLE `cuentas_pagar` ADD `descuento` INT( 10 ) NULL AFTER `subtotal`;

ALTER TABLE `cuentas_pagar` ADD `mensaje` VARCHAR( 100 ) NULL AFTER `abonado` ,
ADD `vendedor` VARCHAR( 60 ) NULL AFTER `mensaje` ,
ADD `cuenta` VARCHAR( 60 ) NULL AFTER `vendedor`;

ALTER TABLE `cuentas_pagar` CHANGE `cod_proveedores` `cod_proveedores` INT( 7 ) NULL DEFAULT NULL;

ALTER TABLE `cuentas_cobrar` CHANGE `cod_clientes` `cod_clientes` INT( 7 ) NULL DEFAULT NULL;

ALTER TABLE `cuentas_pagar` CHANGE `monto_deuda` `monto_deuda` INT( 10 ) NULL DEFAULT NULL ,
CHANGE `subtotal` `subtotal` INT( 10 ) NULL DEFAULT NULL;